import React from 'react';
import RoutingOverlay from "../../layouts/RoutingOverlay/RoutingOverlay";

const Main = () => {
    return (
        <div>
            <RoutingOverlay/>
        </div>
    );
};

export default Main;